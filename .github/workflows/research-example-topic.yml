## EXAMPLE — copy and rename this file for each research topic
## File naming convention: research-{topic-slug}.yml
## Created automatically by /new-research command

# name: Research — Your Topic Name
#
# on:
#   schedule:
#     - cron: '0 6 1 * *'   # Adjust schedule per topic (monthly shown here)
#   workflow_dispatch:
#
# jobs:
#   research:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: write
#
#     steps:
#       - uses: actions/checkout@v4
#         with:
#           token: ${{ secrets.GITHUB_TOKEN }}
#
#       - uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: 'npm'
#
#       - run: npm ci
#
#       - name: Configure git
#         run: |
#           git config user.name  "podvault-bot"
#           git config user.email "podvault-bot@users.noreply.github.com"
#
#       - name: Run research
#         env:
#           LLM_PROVIDER:   ${{ secrets.LLM_PROVIDER }}
#           LLM_API_KEY:    ${{ secrets.LLM_API_KEY }}
#           LLM_MODEL:      ${{ secrets.LLM_MODEL }}
#           TAVILY_API_KEY: ${{ secrets.TAVILY_API_KEY }}
#           RESEARCH_TOPIC: your-topic-slug
#         run: npx tsx src/research/index.ts
